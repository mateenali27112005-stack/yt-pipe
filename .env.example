# OpenAI API Credentials (keep real key in local .env, NEVER commit)
OPENAI_API_KEY=

# Default Provider Selection
TTS_PROVIDER=openai
IMAGE_PROVIDER=openai

# Optional Model Configuration
OPENAI_TTS_MODEL=tts-1
OPENAI_IMAGE_MODEL=gpt-image-2.5-flare
